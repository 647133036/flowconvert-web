# FlowConvert Python 依赖
#
# 仅列必需项。以下三个为可选增强，未安装时程序自动降级，无需手动安装：
#   paddleocr   版面/表格检测（缺失时退回启发式表格检测）
#   easyocr     备用 OCR 引擎（缺失时退回 tesseract / PP-OCR）
#   mtcnnruntime 证件照人脸检测（PyPI 无此包，缺失时改用 models/face/ 的 YuNet，精度更高）
#
# 系统依赖（apt）: ffmpeg poppler-utils tesseract-ocr
#                 tesseract-ocr-chi-sim tesseract-ocr-eng
#
# 安装: python3 -m pip install -r requirements.txt

# 图像处理
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# PDF 处理
pymupdf>=1.24.0

# 文档生成
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23
reportlab>=4.0.0

# OCR
pytesseract>=0.3.10
onnxruntime>=1.17.0
pyclipper>=1.3.0
shapely>=2.0.0
jieba>=0.42.0
opencc-python-reimplemented>=0.1.7

# 证件照抠图 / 换背景去污
rembg>=2.0.50
pymatting>=1.1.0

# 图片矢量化
vtracer>=0.6.1

# 翻译
translatepy>=0.2.0

# HTTP 与解析
requests>=2.28.0
beautifulsoup4>=4.12.0
